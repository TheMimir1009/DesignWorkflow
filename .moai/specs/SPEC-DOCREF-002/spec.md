---
id: SPEC-DOCREF-002
version: "1.1.0"
status: "completed"
created: "2026-01-09"
updated: "2026-01-09"
author: "AI Workflow Team"
priority: "HIGH"
---

# SPEC-DOCREF-002: 문서 참조 패널 UI

## HISTORY

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|----------|
| 1.0.0 | 2026-01-09 | AI Workflow Team | 초안 작성 |
| 1.1.0 | 2026-01-09 | AI Workflow Team | REQ-007 나란히 보기 기능 구현 완료 |

---

## 1. 개요

### 1.1 목적

AI가 Design Document를 생성한 후, 사용자가 이전에 완료된 태스크의 문서들을 탐색하고 참조할 수 있는 사이드 패널 UI를 제공합니다.

### 1.2 범위

- 문서 참조 버튼 및 패널 UI
- 완료 문서 목록 표시
- 검색 및 필터링 UI
- 문서 미리보기 기능
- DocumentEditor/DocumentPreview와의 통합

### 1.3 관련 문서

- PRD: `.moai/project/product.md`
- SPEC-DOCREF-001: 완료 태스크 문서 조회 API
- SPEC-DOCUMENT-001: 설계 문서 초안 생성 및 편집

### 1.4 의존성

- SPEC-DOCREF-001: 완료 문서 조회 API (선행 구현 필요)

---

## 2. 요구사항

### 2.1 기능 요구사항 (EARS 형식)

#### REQ-001: 참조 문서 버튼

**Where** DocumentEditor 또는 DocumentPreview 컴포넌트가 표시될 때,
**the system shall** "참조 문서 보기" 버튼을 제공한다.

**인수 기준:**
- AC-001-1: 버튼이 문서 편집/미리보기 영역 상단에 표시됨
- AC-001-2: 버튼 클릭 시 사이드 패널이 열림
- AC-001-3: 아이콘과 텍스트로 버튼 목적이 명확함

#### REQ-002: 문서 참조 패널

**When** 사용자가 "참조 문서 보기" 버튼을 클릭하면,
**the system shall** 화면 오른쪽에서 슬라이드되어 나오는 사이드 패널을 표시한다.

**인수 기준:**
- AC-002-1: 패널 너비 400px (반응형: 모바일에서 전체 너비)
- AC-002-2: 부드러운 슬라이드 애니메이션 (300ms)
- AC-002-3: 배경 오버레이로 포커스 강조
- AC-002-4: 패널 외부 클릭 또는 X 버튼으로 닫기

#### REQ-003: 완료 문서 목록

**When** 문서 참조 패널이 열리면,
**the system shall** 현재 프로젝트의 완료된 태스크 문서 목록을 표시한다.

**인수 기준:**
- AC-003-1: 태스크 제목, 완료 날짜 표시
- AC-003-2: 문서 타입 아이콘 (Design Doc, PRD, Prototype)
- AC-003-3: 참조 시스템 태그 표시
- AC-003-4: 스크롤 가능한 목록 (무한 스크롤 또는 페이지네이션)
- AC-003-5: 빈 상태 메시지 ("완료된 문서가 없습니다")

#### REQ-004: 검색 기능

**Where** 문서 참조 패널이 열려 있을 때,
**the system shall** 키워드로 문서를 검색할 수 있는 검색창을 제공한다.

**인수 기준:**
- AC-004-1: 검색창이 패널 상단에 고정됨
- AC-004-2: 입력 시 300ms 디바운스로 실시간 검색
- AC-004-3: 검색 결과가 없을 때 안내 메시지 표시
- AC-004-4: 검색어 초기화 버튼 (X)

#### REQ-005: 필터링 기능

**Where** 문서 참조 패널이 열려 있을 때,
**the system shall** 문서 타입 및 참조 시스템으로 필터링할 수 있는 UI를 제공한다.

**인수 기준:**
- AC-005-1: 문서 타입 필터 (Design Doc, PRD, Prototype 체크박스)
- AC-005-2: 필터 적용 시 실시간 목록 업데이트
- AC-005-3: 활성 필터 표시 (뱃지 또는 체크 표시)
- AC-005-4: 필터 초기화 버튼

#### REQ-006: 문서 미리보기

**When** 사용자가 문서 목록에서 항목을 클릭하면,
**the system shall** 해당 태스크의 문서 내용을 미리보기로 표시한다.

**인수 기준:**
- AC-006-1: 클릭한 문서의 상세 내용이 패널 내에서 표시됨
- AC-006-2: 탭으로 문서 타입 전환 (Feature List, Design Doc, PRD, Prototype)
- AC-006-3: 마크다운 렌더링 (GFM 지원)
- AC-006-4: 목록으로 돌아가기 버튼

#### REQ-007: 현재 문서와 함께 보기

**If** 사용자가 미리보기에서 "나란히 보기" 버튼을 클릭하면,
**the system shall** 현재 편집 중인 문서와 참조 문서를 좌우로 나란히 표시한다.

**인수 기준:**
- AC-007-1: 좌: 현재 편집 문서, 우: 참조 문서
- AC-007-2: 분할 비율 조절 가능 (드래그)
- AC-007-3: ESC 키 또는 버튼으로 나란히 보기 종료

---

## 3. UI/UX 설계

### 3.1 컴포넌트 구조

```
DocumentEditor (또는 DocumentPreview)
├── ReferenceDocButton          # 참조 문서 버튼
├── DocumentReferenceSidePanel   # 사이드 패널
│   ├── PanelHeader             # 헤더 (검색, 닫기 버튼)
│   │   └── SearchInput         # 검색창
│   ├── FilterSection           # 필터 영역
│   │   ├── DocumentTypeFilter  # 문서 타입 필터
│   │   └── ActiveFilters       # 활성 필터 표시
│   ├── DocumentList            # 문서 목록
│   │   └── DocumentListItem    # 개별 문서 항목
│   └── DocumentDetailView      # 문서 상세 보기
│       ├── DocumentTabs        # 문서 타입 탭
│       └── DocumentPreview     # 문서 미리보기
└── SideBySideView              # 나란히 보기 (선택적)
```

### 3.2 레이아웃

**기본 상태:**
```
┌─────────────────────────────────────────────────────────┐
│  [참조 문서 보기]                 Document Editor        │
│ ─────────────────────────────────────────────────────── │
│                                                         │
│                    현재 편집 중인 문서                    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**패널 열림 상태:**
```
┌─────────────────────────────────┬───────────────────────┐
│  Document Editor                │ 참조 문서           X │
│ ───────────────────────────────│ ─────────────────────│
│                                 │ [🔍 검색...]         │
│                                 │ ☑ Design  ☐ PRD     │
│    현재 편집 중인 문서           │ ─────────────────────│
│                                 │ 📄 전투 시스템        │
│                                 │    Design Doc • PRD   │
│                                 │    2026-01-08        │
│                                 │ ─────────────────────│
│                                 │ 📄 캐릭터 성장        │
│                                 │    Design Doc        │
│                                 │    2026-01-05        │
└─────────────────────────────────┴───────────────────────┘
```

### 3.3 스타일 가이드

- 색상: 기존 다크 테마 유지 (gray-800, gray-700 배경)
- 폰트: 시스템 폰트 (sans-serif)
- 아이콘: Heroicons 또는 Lucide
- 애니메이션: Tailwind CSS transition 클래스 활용

---

## 4. 기술 설계

### 4.1 새 컴포넌트 목록

```
src/components/reference/
├── ReferenceDocButton.tsx       # 참조 문서 버튼
├── DocumentReferenceSidePanel.tsx  # 메인 사이드 패널
├── ReferenceDocList.tsx         # 문서 목록
├── ReferenceDocListItem.tsx     # 문서 목록 항목
├── ReferenceDocDetail.tsx       # 문서 상세 보기
├── ReferenceDocFilter.tsx       # 필터 UI
└── ReferenceSearchInput.tsx     # 검색 입력
```

### 4.2 상태 관리

```typescript
// src/store/referenceDocStore.ts
interface ReferenceDocState {
  isPanelOpen: boolean;
  documents: CompletedDocumentSummary[];
  selectedDocument: CompletedDocumentDetail | null;
  searchQuery: string;
  filters: {
    documentTypes: ('design' | 'prd' | 'prototype')[];
    referenceIds: string[];
  };
  isLoading: boolean;
  error: string | null;
}

interface ReferenceDocActions {
  openPanel: () => void;
  closePanel: () => void;
  fetchDocuments: (projectId: string) => Promise<void>;
  selectDocument: (taskId: string) => Promise<void>;
  setSearchQuery: (query: string) => void;
  toggleDocumentTypeFilter: (type: 'design' | 'prd' | 'prototype') => void;
  clearFilters: () => void;
}
```

### 4.3 API 연동

```typescript
// src/services/referenceDocService.ts
export async function getCompletedDocuments(
  projectId: string,
  options?: {
    search?: string;
    documentType?: string[];
    reference?: string[];
    includeArchived?: boolean;
  }
): Promise<CompletedDocumentSummary[]>;

export async function getCompletedDocumentDetail(
  projectId: string,
  taskId: string
): Promise<CompletedDocumentDetail>;
```

### 4.4 파일 구조

```
src/
├── components/
│   └── reference/
│       ├── ReferenceDocButton.tsx
│       ├── DocumentReferenceSidePanel.tsx
│       ├── ReferenceDocList.tsx
│       ├── ReferenceDocListItem.tsx
│       ├── ReferenceDocDetail.tsx
│       ├── ReferenceDocFilter.tsx
│       └── ReferenceSearchInput.tsx
├── store/
│   └── referenceDocStore.ts
└── services/
    └── referenceDocService.ts
```

---

## 5. 비기능 요구사항

### 5.1 성능

- NFR-001: 패널 열림 애니메이션 < 300ms
- NFR-002: 문서 목록 초기 로딩 < 500ms
- NFR-003: 검색 응답 시간 < 300ms (디바운스 후)

### 5.2 접근성

- NFR-004: 키보드 네비게이션 지원 (Tab, Enter, ESC)
- NFR-005: 스크린 리더 호환 (ARIA 레이블)
- NFR-006: 충분한 색상 대비 (WCAG AA)

### 5.3 반응형

- NFR-007: 데스크톱 (1024px+): 사이드 패널 400px
- NFR-008: 태블릿 (768px-1023px): 사이드 패널 50%
- NFR-009: 모바일 (<768px): 전체 화면 오버레이

---

## 6. 의존성

### 6.1 내부 의존성

- SPEC-DOCREF-001: 완료 문서 조회 API
- `/src/components/document/DocumentPreview.tsx`: 마크다운 렌더링 재사용
- `/src/store/projectStore.ts`: 현재 프로젝트 ID

### 6.2 외부 의존성

- React 18.x
- Zustand
- TailwindCSS
- react-markdown (DocumentPreview 재사용)

---

## 7. 테스트 시나리오

### TC-001: 패널 열기/닫기

```gherkin
Given DocumentEditor 컴포넌트가 표시되어 있을 때
When 사용자가 "참조 문서 보기" 버튼을 클릭하면
Then 문서 참조 패널이 오른쪽에서 슬라이드되어 나타나야 함

Given 패널이 열려 있을 때
When 사용자가 X 버튼을 클릭하면
Then 패널이 슬라이드되어 닫혀야 함
```

### TC-002: 문서 목록 표시

```gherkin
Given 프로젝트에 완료된 태스크가 3개 있을 때
When 문서 참조 패널이 열리면
Then 3개의 문서 항목이 목록에 표시되어야 함
And 각 항목에 제목, 문서 타입 아이콘, 날짜가 표시되어야 함
```

### TC-003: 검색 기능

```gherkin
Given 패널이 열려 있고 "전투"가 포함된 태스크가 있을 때
When 검색창에 "전투"를 입력하면
Then 300ms 후 "전투"가 포함된 태스크만 표시되어야 함
```

### TC-004: 문서 상세 보기

```gherkin
Given 문서 목록에 항목이 표시되어 있을 때
When 항목을 클릭하면
Then 해당 태스크의 문서 상세 내용이 패널 내에 표시되어야 함
And 문서 타입 탭으로 전환할 수 있어야 함
```

---

## 8. 참고사항

### 8.1 구현 참고

- `/src/components/system/SystemSidebar.tsx`: 사이드바 패턴 참고
- `/src/components/archive/ArchiveDetail.tsx`: 문서 상세 표시 패턴 참고

### 8.2 확장 고려사항

- 향후 SPEC-DOCREF-003 (비교 뷰) 통합 지점 고려
- 드래그 앤 드롭으로 참조 문서 내용 복사 기능 확장 가능
